# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/skoot_transportation?schema=public"

# Alternative database configurations:
# SQLite (for development)
# DATABASE_URL="file:./dev.db"

# PostgreSQL (production)
# DATABASE_URL="postgresql://username:password@localhost:5432/skoot_transportation?schema=public"

# MySQL (alternative)
# DATABASE_URL="mysql://username:password@localhost:3306/skoot_transportation"

# Application Settings
NODE_ENV="development"

# Email Settings (for email notifications)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-email-password"

# Payment Processing (if integrating with payment providers)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Application URL
APP_URL="http://localhost:3000"

# Session Secret (for authentication)
SESSION_SECRET="your-super-secret-session-key"

# File Upload Settings
MAX_FILE_SIZE="10485760" # 10MB in bytes
UPLOAD_DIR="./uploads"